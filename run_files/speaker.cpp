// https://www.arduino.cc/en/Tutorial/toneMelody

void play_note(int intensity) { // Is intensity an int?
    /*
    Experiment with intensity and note
    Example:

    if (100 < intensity < 110) tone(SPEAKER, NOTE_C4);

    or, with a for loop:

    int note[] = {NOTE_C1, NOTE_D1, ...}

    for (int i = 0; i <= 26; i ++) {
        if (i*10 <= intensity < (i+1)*10) tone(SPEAKER, note[i]);
    }
    */
}
